import Registration from "./registration";
import Counter from "./counter";

export default function Welcome() {
    return(
        <section>
            <Counter />
            <Registration />
        </section>
    );
}